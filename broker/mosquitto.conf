listener 1883 0.0.0.0
allow_anonymous true
persistence true
persistence_location /mosquitto/data/

# For production remote access, disable anonymous mode and add password_file.
# Keep this default for local lab/demo use only.
